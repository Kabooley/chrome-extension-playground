import { describe, test, expect, assert } from 'vitest';

describe('Test vitest setup', () => {
    test('Make sure setup is working', () => {
        assert(true === true, 'should be true');
    });
});
